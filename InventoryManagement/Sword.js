class Sword extends Weapon
{
	constructor(name, durability, minDamage, maxDamage, stackable, image)
	{
		super(name, durability, minDamage, maxDamage, stackable, image);
	}
	
	Attack()
	{
		super.Attack();
		
	}
}